while (st.size() != 0)
        // {
        //     string ad = st.top();
        //     str = str + ad;
        //     st.pop();
        //     if (st.size() != 0)
        //         str = str + '.';
        // }